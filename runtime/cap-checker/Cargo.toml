[package]
name = "cap-checker"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Capability enforcement middleware for SourceOS runtime operations"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
