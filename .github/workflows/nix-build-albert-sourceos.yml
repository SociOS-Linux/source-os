name: nix-build-albert-sourceos

on:
  pull_request:
    paths:
      - 'flake.nix'
      - 'flake.lock'
      - '.github/workflows/nix-build-albert-sourceos.yml'
  push:
    branches:
      - main
    paths:
      - 'flake.nix'
      - 'flake.lock'
      - '.github/workflows/nix-build-albert-sourceos.yml'

jobs:
  build-albert-sourceos:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install nix (apt)
        run: sudo apt-get update && sudo apt-get install -y nix-bin

      - name: Build albert-sourceos
        run: |
          set -euo pipefail
          nix --version
          nix build .#albert-sourceos --extra-experimental-features 'nix-command flakes' -L
