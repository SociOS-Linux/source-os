# Builders

This directory is reserved for builder configuration and builder-specific realization.

Examples of intended contents:

- remote Linux builder definitions
- architecture-specific build lanes
- builder inventory fragments
- builder bootstrap helpers

Builder selection and placement semantics should align with `SocioProphet/agentplane` rather than redefining executor meaning locally.
